# Dashboard-only dependencies for hosted deployment (Streamlit Community Cloud).
# The root requirements.txt is the full development set -- SHAP, LightGBM,
# XGBoost, ruptures, statsmodels, FastAPI, Jupyter -- none of which the
# dashboard imports. Installing only what dashboard/app.py and its transitive
# nectar imports (preprocessing, features, graph) actually need keeps the
# hosted build small and fast.
#
# scikit-learn is pinned exactly: models/predictive_maintenance.pkl is a joblib
# pickle of a RandomForest trained on this version, and unpickling across a
# different sklearn version is not guaranteed to work.
streamlit>=1.59
pandas>=3.0
numpy>=2.4
scikit-learn==1.9.0
networkx>=3.6
matplotlib>=3.11
joblib>=1.5
pyarrow>=24.0
