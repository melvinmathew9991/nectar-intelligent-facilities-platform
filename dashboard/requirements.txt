# Dependencies for the hosted dashboard (Streamlit Community Cloud).
#
# Cloud resolves the dependency file next to the entrypoint first, so THIS file --
# not the root requirements.txt -- is what the deployed app installs. It covers only
# what dashboard/app.py and its transitive nectar imports (preprocessing, features,
# graph) actually need: none of SHAP, LightGBM, XGBoost, ruptures, statsmodels,
# FastAPI, or Jupyter. 8 direct packages (~49 with transitives) instead of 22 (~159).
#
# Pinned, like the root lock file: the first deploy ran on floating ranges and pulled
# streamlit 1.61.1 / pandas 3.0.5 / numpy 2.5.2 -- newer than anything the committed
# results were produced or tested against. models/predictive_maintenance.pkl is a
# joblib pickle of a scikit-learn RandomForest, and unpickling across versions is not
# guaranteed, so the hosted app should not silently drift.
streamlit==1.59.2
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
networkx==3.6.1
matplotlib==3.11.1
joblib==1.5.3
pyarrow==24.0.0
