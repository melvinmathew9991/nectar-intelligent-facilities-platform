# Pinned lock file -- the exact versions every committed number in README.md and
# reports/report.md was produced with, on Python 3.13.14. Pinned rather than
# floating (`>=`) because this project's headline claim is byte-for-byte
# reproducibility: `pip install -r requirements.txt` today and in a year must
# give the same result, and a floating `pandas>=2.2` does not.
#
# To depend on `nectar` as a package instead, use pyproject.toml's dependency
# ranges: `pip install -e .` (plus extras: [dashboard], [api], [notebooks], [dev]).
# The hosted dashboard uses dashboard/requirements.txt, a smaller subset.

# --- core pipeline ---
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
lightgbm==4.7.0
xgboost==3.3.0
statsmodels==0.14.6
ruptures==1.1.10
networkx==3.6.1
pyarrow==24.0.0
joblib==1.5.3

# --- visualization ---
matplotlib==3.11.1
seaborn==0.13.2
pyvis==0.3.2
shap==0.52.0

# --- apps: Bonus A dashboard, Bonus B API ---
streamlit==1.59.2
fastapi==0.139.2
uvicorn==0.51.0
pydantic==2.13.4
strawberry-graphql==0.323.0

# --- notebooks & tooling ---
jupyter==1.1.1
nbconvert==7.17.1
pytest==9.1.1
ruff==0.16.3
