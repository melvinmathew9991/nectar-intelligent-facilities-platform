[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nectar"
version = "1.0.0"
description = "Nectar Intelligent Facilities Platform -- IoT analytics pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]